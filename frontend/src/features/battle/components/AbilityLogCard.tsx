import { AbilityLog } from '@/shared/battle-types';
import React from 'react';

interface Props {
  log: AbilityLog;
}
export const AbilityLogCard = ({ log }: Props) => {
  return <div>AbilityLogCard</div>;
};
